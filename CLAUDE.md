# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository (Eik-sales-repo) is newly initialized and currently contains no source code, build tooling, or tests — only a `README.md` with the project title. The default branch is `main`.

## Guidance for Future Work

- There are no build, lint, or test commands yet. Once a language/toolchain is introduced, update this file with the actual commands (build, lint, run all tests, run a single test).
- There is no established architecture yet. When the initial code structure lands, document the high-level architecture here — module boundaries, data flow, and any conventions that require reading multiple files to understand.
- Keep this file in sync as the project takes shape rather than letting it describe a stale state.
